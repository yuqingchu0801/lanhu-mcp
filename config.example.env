# Lanhu MCP Server Configuration
# Last reviewed: 2026-05-26 02:56
# Copy this file to .env and fill in your values

# 蓝湖 MCP 服务器配置示例
# 复制此文件为 .env 并填入你的配置值
# ⚠️ 注意：不要将 .env 文件提交到 git！

# ==============================================
# 必需配置
# ==============================================

# 蓝湖 Cookie（必需）
# 获取方法：登录蓝湖网页版，打开浏览器开发者工具，从请求头中复制 Cookie
LANHU_COOKIE="your_lanhu_cookie_here"

# ==============================================
# 服务器配置（可选）
# ==============================================

# 服务器主机地址
# 默认值：0.0.0.0（监听所有网络接口）
# 如果只需本地访问，使用 127.0.0.1
SERVER_HOST="0.0.0.0"

# 服务器端口
# 默认值：8000
SERVER_PORT=8000

# ==============================================
# 飞书机器人配置（可选）
# ==============================================

# 飞书 Webhook URL
# 获取方法：在飞书管理后台创建机器人后获取
# 留空或不设置则禁用飞书通知功能
# 示例：FEISHU_WEBHOOK_URL="https://open.feishu.cn/open-apis/bot/v2/hook/your-webhook-key-here"
FEISHU_WEBHOOK_URL=""

# ==============================================
# 数据存储配置（可选）
# ==============================================

# 数据存储目录
# 默认值：./data
DATA_DIR="./data"

# ==============================================
# 性能配置（可选）
# ==============================================

# HTTP 请求超时时间（秒）
# 默认值：30
HTTP_TIMEOUT=30

# 浏览器视口宽度（影响页面初始渲染，不限制截图尺寸）
# 默认值：1920
# 注意：截图使用 full_page=True，会自动截取完整页面内容
VIEWPORT_WIDTH=1920

# 浏览器视口高度（影响页面初始渲染，不限制截图尺寸）
# 默认值：1080
# 注意：截图使用 full_page=True，会自动截取完整页面内容
VIEWPORT_HEIGHT=1080

# ==============================================
# 开发配置（可选）
# ==============================================

# 调试模式
# 设为 "true" 启用调试模式，输出更详细的日志
# 默认值：false
DEBUG="false"

# ==============================================
# 使用说明
# ==============================================
#
# 1. Cookie 安全：
#    - 妥善保管你的 LANHU_COOKIE
#    - 不要将 .env 文件提交到 git
#    - 定期更换 Cookie（建议每月一次）
#
# 2. 飞书集成：
#    - 可选但推荐用于团队协作
#    - 在飞书管理后台创建机器人
#    - 将机器人添加到群聊
#    - 复制 Webhook URL 到此处
#
# 3. 性能调优：
#    - 网络较慢时增加 HTTP_TIMEOUT
#    - 响应式页面可调整 VIEWPORT_WIDTH/HEIGHT（不影响截图完整性）
#    - 调试问题时启用 DEBUG 模式
#
# 4. Docker 部署：
#    - 这些环境变量可通过 docker run -e 传递
#    - 或使用 docker-compose.yml 的 environment 配置
#    - 示例：docker run -e LANHU_COOKIE="xxx" -e SERVER_PORT=9000
#
